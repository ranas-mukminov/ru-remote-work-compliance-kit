# 🔒 ru-remote-work-compliance-kit

[![CI](https://github.com/ranas-mukminov/ru-remote-work-compliance-kit/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/ru-remote-work-compliance-kit/actions)
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg)](https://www.python.org/)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

**Комплексный набор инструментов для безопасной удалённой работы с соблюдением требований российского законодательства (152-ФЗ, КИИ, best practices)**

## Описание

`ru-remote-work-compliance-kit` — это открытый репозиторий, предоставляющий шаблоны, скрипты и инструменты автоматизации для организации безопасного окружения удалённой работы в российских компаниях с учётом нормативных требований и лучших практик информационной безопасности.

Этот набор инструментов решает проблему, с которой сталкивается малый и средний бизнес, IT-интеграторы и DevSecOps-команды: необходимость обеспечить безопасность удалённой работы при отсутствии выделенного штата специалистов по ИБ или готовых решений. Проект предоставляет практические, готовые к развёртыванию компоненты для настройки VPN, оценки безопасности рабочих станций, документирования политик и управления рисками.

Проект разработан для организаций, обрабатывающих персональные данные (ПДн) в соответствии с 152-ФЗ, эксплуатирующих объекты критической информационной инфраструктуры (КИИ) по 187-ФЗ, или просто следующих современным практикам безопасности для распределённых команд.

## Основные возможности

- **Шаблоны VPN-конфигураций** — готовые конфиги WireGuard и OpenVPN для сценариев split-tunnel и forced-tunnel, адаптируемые под продакшн
- **Скрипты оценки безопасности рабочих станций** — кроссплатформенная автоматизация (Linux, Windows, macOS) для проверки шифрования дисков, статуса файрвола, обновлений и антивируса
- **Шаблоны политик** — готовые к использованию документы в форматах Markdown/ODT: политики удалённой работы, BYOD, обработки ПДн, правила использования VPN, процедуры реагирования на инциденты, чек-листы защиты рабочих станций
- **AI-генератор политик** — инструмент командной строки для создания кастомизированных политик соответствия на основе профиля вашей организации и оценки рисков
- **Движок оценки рисков** — автоматическая оценка рисков безопасности, специфичных для вашей конфигурации удалённой работы, с приоритизированными рекомендациями
- **CLI-интерфейс** — простой интерфейс командной строки для оценки рабочих станций, генерации политик и формирования отчётов по рискам
- **Ориентация на комплаенс** — создан с учётом требований российского законодательства (152-ФЗ, 187-ФЗ, ТК РФ), оставаясь гибким для общих задач безопасности
- **CI/CD на GitHub Actions** — автоматизированное тестирование, линтинг, проверка типов и сканирование безопасности (bandit, pip-audit) из коробки

## Архитектура и компоненты

Набор инструментов состоит из четырёх основных слоёв:

```
┌─────────────────────────────────────────────────────────┐
│  CLI: ru-remote-kit (интерфейс на базе Typer)           │
└──────────────────┬──────────────────────────────────────┘
                   │
       ┌───────────┴───────────┬──────────────────┐
       ▼                       ▼                  ▼
┌──────────────┐    ┌────────────────────┐  ┌───────────────┐
│  Оценка      │    │   AI Helper        │  │  VPN-конфиги  │
│  рабочих     │    │   (генератор       │  │  и шаблоны    │
│  станций     │    │    политик +       │  │  политик      │
│              │    │    оценка рисков)  │  │               │
└──────────────┘    └────────────────────┘  └───────────────┘
```

**Компоненты:**

1. **CLI (`/cli`)** — приложение командной строки на Typer с тремя основными командами:
   - `assess-endpoint` — проверка безопасности рабочей станции
   - `generate-policy` — создание кастомизированных политик удалённой работы
   - `generate-risk-list` — вывод таблиц оценки рисков

2. **Оценка рабочих станций (`/scripts/endpoints`)** — Bash/PowerShell скрипты для аудита рабочих станций:
   - Шифрование дисков (LUKS на Linux, BitLocker на Windows, FileVault на macOS)
   - Конфигурация файрвола (ufw, iptables, Windows Firewall)
   - Статус системных обновлений
   - Наличие антивируса (где применимо)

3. **AI Helper (`/ai/src`)** — Python-модули для интеллектуальной генерации политик:
   - `profile_loader` — парсинг профилей организаций в форматах YAML/JSON
   - `risk_engine` — оценка рисков на основе отрасли, классификации данных, модели удалённой работы
   - `policy_generator` — рендеринг политик с использованием шаблонов Jinja2

4. **Конфигурации и шаблоны (`/vpn-configs`, `/policies`)** — готовые примеры для использования:
   - Конфигурации серверов и клиентов WireGuard и OpenVPN
   - Шаблоны политик в Markdown (удалённая работа, BYOD, обработка ПДн, использование VPN, реагирование на инциденты, чек-листы защиты)

## Требования

### Системные требования

- **Операционная система:**
  - Linux: Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux 8+ или совместимые дистрибутивы
  - Windows: 10/11 или Server 2019+ (только для скриптов проверки рабочих станций)
  - macOS: 11+ (только для скриптов проверки рабочих станций)

- **Ресурсы:**
  - Минимум: 1 ядро CPU, 512 МБ RAM, 500 МБ дискового пространства
  - Рекомендуется: 2 ядра CPU, 2 ГБ RAM для AI-генерации политик

### Программные зависимости

- **Python:** 3.10 или выше
- **Менеджер пакетов:** pip (входит в состав Python)
- **VPN-софт:** WireGuard и/или OpenVPN (только при развёртывании VPN-инфраструктуры)
- **Оболочка:** Bash 4.0+ (Linux/macOS), PowerShell 5.1+ (Windows)

### Требования к доступу

- **Права:** стандартные пользовательские права для CLI-инструментов; `sudo` или права администратора для скриптов оценки рабочих станций
- **Сеть:** доступ в Интернет для установки Python-пакетов; внешняя сеть не требуется для основного функционала
- **Опционально:** учётная запись GitHub для участия в разработке, интеграции с CI/CD

## Быстрый старт (TL;DR)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit

# 2. Установить Python-зависимости
pip install -e .

# 3. Проверить безопасность вашей рабочей станции
ru-remote-kit assess-endpoint --format json --output my-endpoint-report.json

# 4. Сгенерировать политику удалённой работы для вашей организации
ru-remote-kit generate-policy \
  --profile examples/org-profiles/smb-it-outsourcing.yaml \
  --out my-remote-work-policy.md

# 5. Просмотреть оценку рисков
ru-remote-kit generate-risk-list \
  --profile examples/org-profiles/smb-it-outsourcing.yaml
```

Расположение выходных файлов по умолчанию: текущая директория, если не указано `--output` или `--out`.

## Подробная установка

### Установка на Ubuntu / Debian

```bash
# Обновить индекс пакетов
sudo apt update

# Установить Python 3.10+, если ещё не установлен
sudo apt install -y python3 python3-pip git

# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit

# Установить в режиме editable (рекомендуется для разработки)
pip install -e .

# ИЛИ установить как пакет
pip install .

# Проверить установку
ru-remote-kit --help
```

### Установка на RHEL / Rocky Linux / Alma Linux

```bash
# Установить Python 3.10+ и Git
sudo dnf install -y python3 python3-pip git

# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit

# Установить пакет
pip install -e .

# Проверить
ru-remote-kit --help
```

### Установка на macOS

```bash
# Установить Homebrew, если ещё не установлен (https://brew.sh)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Python 3.10+
brew install python@3.10 git

# Клонировать и установить
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit
pip3 install -e .
ru-remote-kit --help
```

### Установка на Windows

```powershell
# Установить Python 3.10+ с https://www.python.org/downloads/
# При установке отметить "Add Python to PATH"

# Установить Git с https://git-scm.com/download/win

# Клонировать репозиторий (в PowerShell или CMD)
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit

# Установить пакет
pip install -e .

# Проверить
ru-remote-kit --help
```

### Установка для разработки

Для участников разработки и тех, кто хочет запускать тесты:

```bash
# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit

# Установить с зависимостями для разработки
pip install -e .
pip install pytest pytest-mock flake8 mypy bandit pip-audit types-PyYAML types-requests

# Установить shellcheck (для проверки shell-скриптов)
# Ubuntu/Debian:
sudo apt install -y shellcheck

# macOS:
brew install shellcheck

# Запустить тесты
pytest

# Запустить все проверки качества
chmod +x scripts-dev/*.sh
./scripts-dev/dev_run_all_tests.sh
```

## Настройка

### Профиль организации

Создайте YAML-файл с описанием вашей организации для работы AI-генератора политик и оценки рисков. Пример:

```yaml
# example-org-profile.yaml
organization:
  name: "ООО «Примерные IT-сервисы»"
  industry: "IT Services & Consulting"
  size: 50  # количество сотрудников
  
remote_work:
  model: "hybrid"  # remote-first | hybrid | occasional
  allow_byod: true
  personal_data_processing: true
  data_classification:
    - "public"
    - "internal"
    - "confidential"
    - "personal_data"

compliance_requirements:
  - "152-FZ"  # Закон о персональных данных
  - "187-FZ"  # Критическая информационная инфраструктура

vpn:
  required: true
  type: "split-tunnel"  # split-tunnel | forced-tunnel

endpoints:
  os_allowed:
    - "Linux"
    - "Windows"
    - "macOS"
  encryption_required: true
  antivirus_required: true
```

Сохраните как `my-org-profile.yaml` и используйте:

```bash
ru-remote-kit generate-policy --profile my-org-profile.yaml --out policy.md
```

### Конфигурация VPN

#### Пример WireGuard Split-Tunnel

Отредактируйте `vpn-configs/wireguard/wg-split-tunnel.conf.example`:

```ini
[Interface]
PrivateKey = <SERVER_PRIVATE_KEY>
Address = 10.8.0.1/24
ListenPort = 51820
PostUp = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE

[Peer]
# Клиент 1
PublicKey = <CLIENT_PUBLIC_KEY>
AllowedIPs = 10.8.0.2/32
```

Генерация ключей:
```bash
umask 077
wg genkey | tee server-privatekey | wg pubkey > server-publickey
wg genkey | tee client-privatekey | wg pubkey > client-publickey
```

Развёртывание на сервере:
```bash
sudo cp wg-split-tunnel.conf /etc/wireguard/wg0.conf
sudo systemctl enable wg-quick@wg0
sudo systemctl start wg-quick@wg0
```

#### Пример OpenVPN

См. `vpn-configs/openvpn/server.conf.example` для детальной конфигурации сервера OpenVPN. Основные настройки:

```conf
port 1194
proto udp
dev tun
ca /etc/openvpn/ca.crt
cert /etc/openvpn/server.crt
key /etc/openvpn/server.key
dh /etc/openvpn/dh2048.pem
server 10.8.0.0 255.255.255.0
push "route 192.168.1.0 255.255.255.0"
keepalive 10 120
cipher AES-256-GCM
auth SHA256
user nobody
group nobody
```

Генерация сертификатов с помощью Easy-RSA (подробные инструкции в `vpn-configs/openvpn/README.ru.md`).

### Шаблоны политик

Готовые шаблоны политик доступны в `policies/templates/`:

- `remote-work-policy.ru.md` — общая политика удалённой работы
- `byod-policy.ru.md` — политика использования личных устройств
- `pdl-home-handling-policy.ru.md` — обработка персональных данных на дому
- `vpn-usage-rules.ru.md` — требования к использованию VPN
- `incident-reporting-procedure.ru.md` — процедура реагирования на инциденты безопасности
- `endpoint-hardening-checklist.ru.md` — чек-лист защиты рабочих станций

Скопируйте и адаптируйте эти шаблоны под вашу организацию, или используйте AI-генератор для автоматической кастомизации на основе вашего профиля.

## Использование и типовые задачи

### Оценка безопасности рабочей станции

Запуск проверок безопасности на текущей рабочей станции:

```bash
# Вывод в JSON (машиночитаемый формат)
ru-remote-kit assess-endpoint --format json --output endpoint-report.json

# Вывод в консоль в читаемом виде
ru-remote-kit assess-endpoint --format text

# Проверка с сохранением по конкретному пути
ru-remote-kit assess-endpoint --format json --output /var/log/compliance/$(hostname)-$(date +%Y%m%d).json
```

Проверка включает:
- Статус шифрования дисков
- Конфигурацию файрвола
- Статус системных обновлений
- Версию ОС и имя хоста
- Временную метку проверки

### Генерация кастомизированных политик

Создание политики удалённой работы, адаптированной под вашу организацию:

```bash
# Используя пример профиля
ru-remote-kit generate-policy \
  --profile examples/org-profiles/smb-it-outsourcing.yaml \
  --out custom-policy.md

# Используя ваш собственный профиль
ru-remote-kit generate-policy \
  --profile /path/to/your-org-profile.yaml \
  --out /docs/policies/remote-work-2024.md
```

Сгенерированная политика включает разделы:
- Область применения и определения
- Разрешённые места работы
- Требования к устройствам
- VPN и сетевая безопасность
- Обработка персональных данных
- Реагирование на инциденты
- Ответственность

### Просмотр оценки рисков

Генерация приоритизированного списка рисков безопасности:

```bash
ru-remote-kit generate-risk-list --profile examples/org-profiles/smb-it-outsourcing.yaml
```

Пример вывода:
```
| Риск | Приоритет | Рекомендация |
|---|---|---|
| Незашифрованные устройства с ПДн | HIGH | Внедрить полнодисковое шифрование на всех рабочих станциях |
| Отсутствие VPN для удалённых работников | HIGH | Развернуть WireGuard с forced-tunnel для доступа к ПДн |
| BYOD без MDM | MEDIUM | Внедрить условный доступ или предоставить корпоративные устройства |
```

### Развёртывание VPN-инфраструктуры

**Развёртывание WireGuard:**

```bash
# На сервере (Ubuntu/Debian)
sudo apt install -y wireguard

# Генерация серверных ключей
cd vpn-configs/wireguard
umask 077
wg genkey | tee server.key | wg pubkey > server.pub

# Настройка (отредактируйте wg-split-tunnel.conf.example с вашими ключами)
sudo cp wg0.conf /etc/wireguard/
sudo systemctl enable wg-quick@wg0
sudo systemctl start wg-quick@wg0

# Проверка статуса
sudo wg show
```

**Развёртывание OpenVPN:**

```bash
# Установка OpenVPN и Easy-RSA
sudo apt install -y openvpn easy-rsa

# Настройка PKI (см. vpn-configs/openvpn/README.ru.md для полной инструкции)
make-cadir ~/openvpn-ca
cd ~/openvpn-ca
./easyrsa init-pki
./easyrsa build-ca
./easyrsa gen-dh
./easyrsa build-server-full server nopass

# Развёртывание конфига
sudo cp vpn-configs/openvpn/server.conf.example /etc/openvpn/server.conf
# (скорректируйте пути в server.conf)
sudo systemctl enable openvpn@server
sudo systemctl start openvpn@server
```

### Прямой запуск скриптов оценки рабочих станций

Без установки Python-пакета можно запускать отдельные скрипты:

**Linux:**
```bash
chmod +x scripts/endpoints/check_linux.sh
./scripts/endpoints/check_linux.sh > report.json
```

**Windows (PowerShell от имени Администратора):**
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\scripts\endpoints\check_windows.ps1 | Out-File -Encoding utf8 report.json
```

**macOS:**
```bash
chmod +x scripts/endpoints/check_macos.sh
./scripts/endpoints/check_macos.sh > report.json
```

## Обновление

### Обновление набора инструментов

```bash
# Перейти в директорию репозитория
cd /path/to/ru-remote-work-compliance-kit

# Получить последние изменения
git pull origin main

# Переустановить пакет для обновления CLI и зависимостей
pip install -e . --upgrade

# Проверить версию (посмотреть хеш коммита)
git log -1 --oneline
```

### Обновление Python-зависимостей

```bash
# Обновить все зависимости до последних совместимых версий
pip install -e . --upgrade --upgrade-strategy eager

# ИЛИ обновить конкретные зависимости
pip install --upgrade pyyaml pydantic typer jinja2
```

### Обновление шаблонов политик

Шаблоны политик версионируются вместе с репозиторием. После `git pull` просмотрите изменения:

```bash
# Проверить, что изменилось в шаблонах
git diff HEAD~1 policies/templates/

# Скопировать обновлённые шаблоны в вашу директорию с кастомизированными политиками при необходимости
cp policies/templates/remote-work-policy.ru.md /your/docs/policies/
```

**Критические изменения:** Крупные обновления будут отмечены в сообщениях коммитов и CHANGELOG.md (если присутствует). Всегда проверяйте diff перед развёртыванием обновлённых VPN-конфигов или политик в продакшн.

## Логи, мониторинг, устранение неполадок

### Логи

**Логи оценки рабочих станций:**
- Скрипты выводят JSON в stdout; перенаправьте в файл для логирования:
  ```bash
  ./scripts/endpoints/check_linux.sh >> /var/log/compliance-checks/$(date +%Y%m%d).log 2>>/var/log/compliance-checks/errors.log
  ```

**Логи VPN:**
- WireGuard: `sudo journalctl -u wg-quick@wg0 -f`
- OpenVPN: `sudo journalctl -u openvpn@server -f` или проверьте `/var/log/openvpn/`

**Ошибки Python CLI:**
- Ошибки CLI выводятся в stderr
- Для подробного вывода добавьте логирование отладки (модифицируйте исходный код CLI или установите переменные окружения, если поддерживается)

### Частые проблемы

**Проблема:** `ru-remote-kit: command not found`

**Решение:**
```bash
# Убедитесь, что директория bin Python в PATH
export PATH="$HOME/.local/bin:$PATH"

# Или переустановите с флагом --user
pip install --user -e .

# Добавьте в профиль оболочки для постоянства
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

---

**Проблема:** Скрипт оценки рабочей станции возвращает `permission denied`

**Решение:**
```bash
# Сделайте скрипты исполняемыми
chmod +x scripts/endpoints/*.sh
chmod +x scripts/endpoints/*.ps1

# Запускайте с sudo для системных проверок
sudo ./scripts/endpoints/check_linux.sh
```

---

**Проблема:** `ModuleNotFoundError: No module named 'remote_work_ai_helper'`

**Решение:**
```bash
# Убедитесь, что модули ai/src в Python path
export PYTHONPATH="${PYTHONPATH}:$(pwd):$(pwd)/ai/src"

# ИЛИ переустановите пакет
pip uninstall ru-remote-work-compliance-kit
pip install -e .
```

---

**Проблема:** VPN-подключение не работает с ошибкой `Permission denied (publickey)`

**Решение:**
- WireGuard: проверьте, что ключи правильно размещены в конфиге, убедитесь, что файрвол разрешает UDP 51820
  ```bash
  sudo ufw allow 51820/udp
  sudo systemctl restart wg-quick@wg0
  ```
- OpenVPN: проверьте пути к сертификатам, убедитесь, что у клиента корректный `.ovpn` файл с встроенными сертификатами

---

**Проблема:** Генерация политики падает с `KeyError` или ошибкой парсинга YAML

**Решение:**
```bash
# Валидируйте синтаксис YAML
python3 -c "import yaml; yaml.safe_load(open('your-profile.yaml'))"

# Проверьте обязательные поля в профиле (см. раздел Настройка)
# Убедитесь, что присутствуют organization.name, remote_work.model, compliance_requirements
```

---

**Проблема:** Нет данных в отчёте рабочей станции (пустые проверки)

**Решение:**
- **Linux:** скрипту может потребоваться `sudo` для `lsblk`, `ufw`, `iptables`
- **Windows:** запускайте PowerShell от имени Администратора
- **macOS:** предоставьте Терминалу полный доступ к диску в Системные настройки > Безопасность и конфиденциальность
- Проверьте наличие требуемых команд (например, `lsblk`, `ufw`, `iptables`, `Get-BitLockerVolume`)

## Замечания по безопасности

> [!CAUTION]
> **Этот набор инструментов НЕ обеспечивает автоматического соответствия законодательству.** Он предоставляет инструменты и шаблоны; реальное соответствие требует юридической экспертизы, организационного внедрения политик и постоянного мониторинга.

### Рекомендации по безопасности

1. **Меняйте учётные данные и ключи по умолчанию:**
   - Всегда генерируйте уникальные VPN-ключи и сертификаты для каждого развёртывания
   - Никогда не коммитьте приватные ключи или пароли в систему контроля версий
   - Используйте `.gitignore` для исключения чувствительных файлов (`.key`, `.pem`, `.ovpn` со встроенными сертификатами)

2. **Ограничивайте доступ к VPN и интерфейсам управления:**
   - Не выставляйте порты управления VPN (WireGuard 51820, OpenVPN 1194) напрямую в Интернет без правил файрвола
   - Используйте fail2ban или аналогичные инструменты для защиты от brute-force атак
   - Рассмотрите размещение VPN за reverse proxy или файрволом с гео-блокировкой для соответствия российским требованиям

3. **Ограничивайте доступ к веб-интерфейсам и API:**
   - При развёртывании любых веб-дашбордов или API (будущий функционал) используйте только HTTPS
   - Внедрите аутентификацию (BasicAuth, OAuth, клиентские сертификаты)
   - Используйте VPN или IP-whitelist для ограничения доступа

4. **Шифруйте чувствительные данные:**
   - Храните профили организаций, содержащие классификацию ПДн или данные о внутренней сети, зашифрованными в покое
   - Используйте ansible-vault, SOPS или аналогичные инструменты для управления секретами в CI/CD

5. **Регулярные обновления:**
   - Поддерживайте Python-зависимости в актуальном состоянии для закрытия уязвимостей
   - Подпишитесь на оповещения о безопасности для WireGuard, OpenVPN и Python-пакетов
   - Запускайте `pip-audit` регулярно: `pip install pip-audit && pip-audit`

6. **Аудит и логирование:**
   - Включите логирование доступа к VPN для соответствия (требуется 152-ФЗ при обработке ПДн)
   - Централизуйте отчёты оценки рабочих станций для периодического анализа
   - Внедрите политики ротации и хранения логов (проконсультируйтесь с юристами по требованиям хранения данных согласно российскому законодательству)

7. **Принцип минимальных привилегий:**
   - Запускайте VPN-процессы от имени непривилегированных пользователей где возможно (OpenVPN поддерживает `user nobody`)
   - Ограничьте sudo-доступ для скриптов оценки рабочих станций конкретными командами

8. **Юридическое соответствие:**
   - Проконсультируйтесь с юристами перед развёртыванием политик или обработкой персональных данных
   - Убедитесь, что логирование VPN и хранение данных соответствуют 152-ФЗ и трудовому законодательству
   - Ознакомьтесь с [LEGAL.md](LEGAL.md) для дисклеймеров и запрещённых сценариев использования

## Структура проекта

```
ru-remote-work-compliance-kit/
├── ai/                          # AI-генератор политик
│   └── src/remote_work_ai_helper/
│       ├── profile_loader.py    # Парсинг YAML/JSON профилей
│       ├── risk_engine.py       # Логика оценки рисков
│       └── policy_generator.py  # Рендеринг политик на Jinja2
├── cli/                         # Интерфейс командной строки
│   ├── main.py                  # Точка входа Typer приложения
│   └── commands/                # Модули CLI-команд
│       └── assess_endpoint.py   # CLI оценки рабочих станций
├── examples/                    # Примеры профилей организаций
│   └── org-profiles/
│       ├── smb-it-outsourcing.yaml
│       └── fintech-remote-first.yaml
├── policies/                    # Шаблоны политик
│   └── templates/
│       ├── remote-work-policy.ru.md
│       ├── byod-policy.ru.md
│       ├── pdl-home-handling-policy.ru.md
│       ├── vpn-usage-rules.ru.md
│       ├── incident-reporting-procedure.ru.md
│       └── endpoint-hardening-checklist.ru.md
├── scripts/                     # Отдельные скрипты оценки
│   ├── endpoints/
│   │   ├── check_linux.sh
│   │   ├── check_windows.ps1
│   │   └── check_macos.sh
│   └── reporting/               # (Планируется: скрипты агрегации)
├── scripts-dev/                 # Скрипты разработки и CI
│   └── dev_run_all_tests.sh    # Запуск всех тестов и линтеров
├── tests/                       # Набор тестов pytest
├── vpn-configs/                 # Примеры VPN-конфигураций
│   ├── wireguard/
│   │   ├── wg-split-tunnel.conf.example
│   │   └── wg-forced-tunnel.conf.example
│   └── openvpn/
│       ├── server.conf.example
│       ├── client.conf.example
│       └── README.ru.md
├── .github/workflows/           # CI/CD на GitHub Actions
│   ├── ci.yml                   # Основной CI pipeline
│   └── security.yml             # Сканирование безопасности (bandit, pip-audit)
├── CONTRIBUTING.md              # Правила участия в разработке
├── LEGAL.md                     # Юридические дисклеймеры
├── LICENSE                      # Лицензия Apache 2.0
├── pyproject.toml               # Конфигурация Python-пакета
├── README.md                    # Английская версия
└── README.ru.md                 # Этот файл
```

## Как внести вклад

Мы приветствуем участие сообщества! Вот как можно помочь:

### Сообщение о проблемах

- Используйте [GitHub Issues](https://github.com/ranas-mukminov/ru-remote-work-compliance-kit/issues)
- Предоставьте чёткое описание, шаги воспроизведения и детали окружения (ОС, версия Python)
- Для уязвимостей безопасности см. [SECURITY.md](SECURITY.md) (если присутствует) или свяжитесь с автором в частном порядке

### Предложение изменений

1. Форкните репозиторий
2. Создайте ветку фичи: `git checkout -b feature/my-improvement`
3. Внесите изменения, следуя стилю кода:
   - Python: PEP 8, type hints, docstrings
   - Shell-скрипты: соответствие ShellCheck, POSIX где возможно
   - YAML: 2-пробельная индентация
4. Напишите или обновите тесты для ваших изменений
5. Запустите проверки качества: `./scripts-dev/dev_run_all_tests.sh`
6. Коммитьте с понятными сообщениями: `git commit -m "Add: поддержка Fedora в check_linux.sh"`
7. Запушьте и откройте Pull Request

### Настройка для разработки

```bash
git clone https://github.com/ranas-mukminov/ru-remote-work-compliance-kit.git
cd ru-remote-work-compliance-kit
pip install -e .
pip install pytest pytest-mock flake8 mypy bandit types-PyYAML types-requests
chmod +x scripts-dev/*.sh
./scripts-dev/dev_run_all_tests.sh
```

Все PR должны проходить проверки CI (тесты, линтинг, проверка типов, сканирование безопасности).

## Лицензия

Этот проект лицензирован под **Apache License 2.0**.

Вы можете свободно использовать, изменять и распространять это программное обеспечение в коммерческих и некоммерческих целях при условии включения оригинальной лицензии и уведомления об авторских правах.

Подробности см. в [LICENSE](LICENSE).

## Автор и коммерческая поддержка

**Автор:** [Ранас Мукминов](https://github.com/ranas-mukminov)

Этот проект разработан и поддерживается опытным DevOps/DevSecOps инженером, специализирующимся на безопасной инфраструктуре для российских организаций.

### Коммерческие услуги

Для продакшн-внедрений, разработки кастомизированных политик, аудитов безопасности и постоянной поддержки доступны коммерческие услуги через [run-as-daemon.ru](https://run-as-daemon.ru).

**Мы предоставляем:**
- **Консалтинг по соответствию** — gap-анализ, выравнивание под 152-ФЗ, оценка готовности к КИИ
- **Настройка VPN-инфраструктуры** — развёртывание WireGuard/OpenVPN, высокодоступные конфигурации, интеграция с мониторингом
- **Автоматизация безопасности рабочих станций** — интеграция с SIEM, MDM и дашбордами соответствия
- **Разработка кастомизированных политик** — политики под вашу отрасль, координация с юристами, материалы для обучения сотрудников
- **Планирование реагирования на инциденты** — runbook'и, настольные учения, интеграция с SOC-процессами
- **Интеграция DevSecOps-процессов** — security gates в CI/CD, аудит infrastructure-as-code, управление секретами

Контакты: посетите [run-as-daemon.ru](https://run-as-daemon.ru) или свяжитесь через [GitHub профиль](https://github.com/ranas-mukminov).

---

**Дисклеймер:** Этот проект не является юридической консультацией. Соответствие законам Российской Федерации требует юридического анализа, специфичного для вашей организации. Проконсультируйтесь с вашими юридическими и ИБ-командами перед развёртыванием в продакшн. Полный дисклеймер см. в [LEGAL.md](LEGAL.md).
